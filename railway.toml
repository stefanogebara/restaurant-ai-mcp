[build]
builder = "nixpacks"

[build.nixpacks]
installCmd = "npm install --production"

[deploy]
startCommand = "node api/voice-server/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
