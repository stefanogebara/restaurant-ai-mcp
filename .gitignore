node_modules
.vercel
.env
.env.local
.env.*
*.log
.claude/settings.local.json
gcp-credentials.json
*-credentials.json

# E2E auth state (contains JWTs and tokens)
e2e/auth-state.json
e2e/auth-state-local.json

# Test/debug files with potential sensitive data
check-*.js
check-*.py
test-*.js
debug-*.js
*-debug.js
*-test.js

# Security - never commit these
*.pem
*.key
secrets.json
credentials.json

# Training data - exclude from git
hotel_bookings.csv
ml-training-data/
ml-training/
scripts/train-ml-model.py

# Keep ml_models/ but it's tracked
# Keep model_metadata.json, feature_importance.csv (small files)

# Lambda deployment artifacts
lambda/package/
lambda/lambda-package.zip
lambda/trust-policy.json
.env*.local

# Local scripts and one-off utilities (exclude by name, not directory)
scripts/seed-*
scripts/create-*
scripts/test-*
scripts/fix-*
scripts/check-*
scripts/generate-*
# CI-tracked smoke tests (allowlist exceptions to scripts/test-*)
!scripts/test-slow-network.js
!scripts/test-admin-pages.js
scripts/setup-*
scripts/train-ml-model.py

# Database migration drafts
database/

# Planning docs
docs/plans/

# Test/CI output files
test-output.txt
test-results.txt
playwright-output.txt
playwright-results.txt
playwright-report/
coverage/

# Agent workspace
parallel-agents/

# Windows artifact
nul
.env.outreach
.mcp.json
.gstack/

# Temp / E2E artifacts (Chrome profiles, screenshots, reports — can reach GBs)
.tmp/
.playwright-mcp/
CUsersstefa*.txt

# Screenshot dirs written by Playwright specs + ad-hoc scripts
audit-screenshots/
e2e/screenshots/
test-results/

# Supabase CLI local state
supabase/.temp/

# Stored Playwright auth state (sessions with JWTs, OAuth cookies)
playwright/

# Root-level screenshots from ad-hoc Playwright/debug sessions
/*.png
/bash.exe.stackdump
/nul.*
/C꠷*
"/C\ufe36Users*"

# Design audit screenshots — regenerable via scripts/design-audit-visual.mjs
design-audit/**/*.png
