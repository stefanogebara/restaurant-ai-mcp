# Seatable Platform Audit — 2026-03-15

## Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 3 |
| HIGH | 8 |
| MEDIUM | 12 |
| LOW | 10 |

All tests pass (1,434 backend + 481 frontend). Build clean. Auth guards solid.

## CRITICAL (3)

### ~~C-01: seatable.one serves stale deployment (blank page)~~ ✅ FIXED
Redeployed via push to main. Production returns 200.

### ~~C-02: JWT tokens committed in git-tracked e2e auth state~~ ✅ ALREADY FIXED
File removed from git tracking (commit d8527651), already in .gitignore.

### ~~C-03: JWT_SECRET missing from .env.example~~ ✅ ALREADY FIXED
JWT_SECRET (line 14) and SUPABASE_JWT_SECRET (line 15) already in .env.example.

## HIGH (8)

### ~~H-01: Cross-tenant data leak in customer-reservation.js~~ ✅ ALREADY FIXED
Phone lookups already require restaurant_id (line 65-73).

### ~~H-02: Twilio SMS webhook skips signature verification~~ ✅ ALREADY FIXED
api/twilio-sms-webhook.js already rejects with 403 when TWILIO_AUTH_TOKEN is missing (line 150-151) and validates signature (line 155-157).

### ~~H-03: Error messages leak internal details to clients~~ ✅ ALREADY FIXED
All 8 files already return generic messages ("Internal server error", "Something went wrong") and log real errors server-side.

### ~~H-04: ElevenLabs widget CORS error on landing page~~ ✅ ALREADY FIXED
Script self-hosted at /js/convai-widget-embed.js — no unpkg.com dependency.

### ~~H-05: Large blank sections on landing page~~ ✅ FIXED
Video files didn't exist at /videos/*.mp4 but videoSrc was set, rendering empty black VideoPlayer. Set to null to show feature placeholders instead.

### ~~H-06: Dead Airtable code still active~~ ✅ FIXED
api/_lib/customer-history.js (479 lines) — deprecated Nov 2025, still required by 3 files. File deleted, imports updated.

### ~~H-07: pre-reservation-upsell cron missing from health monitor~~ ✅ FALSE POSITIVE
Already in CRON_JOBS array at health.js:40.

### ~~H-08: npm audit — dompurify XSS + axios DoS + undici issues~~ ✅ ALREADY FIXED
0 vulnerabilities in both root and client packages.

## MEDIUM (12)

### M-01: Demo dashboard redirect loops (~10-15s then redirects away)
### M-02: Inconsistent 401 error response format (5 different formats)
### M-03: Main JS chunk 1,368 KB (over 500 KB limit)
### M-04: Unreachable Express Router files (api/routes/)
### M-05: 9 authenticated endpoints lack rate limiting
### M-06: Rate limiter fails open on Redis error
### M-07: Airtable formula injection in check-availability.js
### M-08: 17 env vars used but missing from .env.example
### M-09: supabase.js is 2,052 lines (guideline: 800 max)
### M-10: whatsapp-webhook.js (1,459) and host-dashboard.js (1,315) oversized
### M-11: secureHandler wrapper exists but underused
### M-12: Vite Windows path traversal (dev-time only)

## LOW (10)

L-01: /book/test-restaurant-ai redirects to /demo/setup
L-02: /customer route redirects unpredictably
L-03: Multiple blank about:blank tabs accumulate
L-04: i18n mixed English/Portuguese, missing accents
L-05: "Voltar ao inicio" link points to broken / route
~~L-06: CLAUDE.md test counts outdated~~ ✅ FIXED — updated to 1434+/458+
L-07: voice-server/ directory may be dead code
L-08: customer_portal rate limit undefined (falls back to 60/min)
L-09: ml-training-status.js uses manual CORS
~~L-10: Password field uses type="text"~~ ✅ FALSE POSITIVE — Login.tsx line 271 uses type="password"

## UI/UX Issues (from responsive review agent)

### CRITICAL (additional)
- ~~C-04: Language switches unpredictably between EN/PT~~ ✅ FIXED — demo preset visits overwrote i18nextLng permanently. Fixed via changeDemoLanguage() + useRef save/restore on unmount.

### HIGH (additional)
- ~~H-09: Pricing mismatch~~ ✅ BY DESIGN — detectCurrency() shows BRL for Brazilian users, EUR otherwise. Both price sets are correct.
- ~~H-10: Video section shows gray placeholder~~ ✅ BY DESIGN — videoSrc=null shows feature placeholders. Actual video assets needed (content task, not code bug).
- ~~H-11: Demo dashboard names truncated on mobile~~ ✅ FIXED — replaced `truncate` with `break-words` on customer name div
- ~~H-12: FAB button overlaps content on mobile~~ ✅ FIXED — increased mobile bottom padding from pb-16 to pb-28

### MEDIUM (additional)
- M-13: Pricing cards don't stack on mobile (require horizontal scroll)
- M-14: Scroll-to-top button overlaps footer copyright
- ~~M-15: Missing accent marks in Portuguese~~ ✅ ALREADY FIXED — words not found unaccented in pt-BR.json
- M-16: "Valor da conte" truncated in active parties panel (should be "conta")
- M-17: 66+ console errors on demo/setup from failing PostHog/Sentry polling
- M-18: Page title inconsistency (mixed EN/PT across routes)

### LOW (additional)
- L-11: No visible hamburger menu icon on mobile
- L-12: Demo restaurant cards show "IT"/"JP"/"MX" text instead of flag emojis
- ~~L-13: Mobile nav items slightly under 44px touch target~~ ✅ FIXED — LandingNav hamburger now min-w/h-[44px], PricingSection enterprise link min-h-[44px]
- ~~L-14: "Powered by ElevenLabs" text visible when voice demo is broken~~ ✅ FIXED — conditionally hidden when scriptError

## UPDATED TOTALS

| Severity | Count |
|----------|-------|
| CRITICAL | 4 |
| HIGH | 12 |
| MEDIUM | 18 |
| LOW | 14 |
| TOTAL | 48 |

## Frontend E2E Issues (from Playwright agent)

### HIGH (additional)
- ~~H-13: /demo/setup entirely in English when lang=PT~~ ✅ FIXED — wired hardcoded strings in DemoSetupPage.tsx + DemoVoiceAgent.tsx through useTranslation(), added keys to en/pt-BR/es.json. Also fixed ES locale (had Portuguese mixed in).
- ~~H-14: Language state corruption~~ ✅ FIXED — useDemoLocale now saves/restores original language via changeDemoLanguage() + useRef
- ~~H-15: Landing page mock dashboard preview hardcoded English~~ ✅ FIXED — added useTranslation() to DashboardSyncAnimation.tsx, keys in en/pt-BR/es.json

### MEDIUM (additional)
- ~~M-19: Demo restaurant cards show "Italian"/"Japanese"/"Mexican" instead of PT translations~~ ✅ ALREADY FIXED — uses t(cuisineKey) with PT translations
- ~~M-20: Voice demo suggestion pills hardcoded in English~~ ✅ FIXED
- ~~M-21: WhatsApp preview message hardcoded in English~~ ✅ ALREADY FIXED — uses t('landing.whatsapp.previewMessage')
- M-22: Revenue Forecast day labels always EN (staffingService.js hardcodes 'en-US' locale)
- ~~M-23: Missing accent in "VEJA EM ACAO"~~ ✅ ALREADY FIXED — string not found in pt-BR.json

### LOW (additional)
- ~~L-15: Skip link always shows Portuguese "Pular para o conteudo" even in EN mode~~ ✅ ALREADY FIXED — uses t('common.skipToContent')
- L-16: Login page sidebar stats truncated at 375px
- ~~L-17: UsageStatsCard has hardcoded "Usage This Month" string~~ ✅ ALREADY FIXED — uses t('dashboard.usage.title')

## ROOT CAUSE PATTERN
The #1 category across all agents is **incomplete i18n**. Phase 12A redesigned the demo setup form with hardcoded English, bypassing the existing i18n system. The mock dashboard, voice pills, and WhatsApp preview all have hardcoded EN content never wired through useTranslation().

## FINAL TOTALS

| Severity | Count |
|----------|-------|
| CRITICAL | 4 |
| HIGH | 15 |
| MEDIUM | 23 |
| LOW | 17 |
| TOTAL | 59 |
