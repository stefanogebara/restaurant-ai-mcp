# Seatable Platform Audit — 2026-04-12

5 parallel agents: Backend, E2E, UI/UX, Security, Features

---

## CRITICAL (fix today)

### C-1 — Hardcoded Supabase ANON_KEY in source
File: reels-toolkit/scripts/record-scenes.js:22
Real production key (ckforlwdhewexyqljsaf) hardcoded. Never expires (exp 2086).
Rotate in Supabase dashboard -> Project Settings -> API -> Regenerate ANON key.
Replace with process.env.SUPABASE_ANON_KEY.

### C-2 — Forgeable HMAC via fallback-secret
Files: api/campaign-unsubscribe.js:27, api/services/automatedCampaignService.js:347
const secret = process.env.CRON_SECRET || 'fallback-secret';
Anyone can forge valid unsubscribe links. Remove fallback — fail hard if CRON_SECRET missing.

---

## HIGH

### H-1 — Expired WhatsApp number on landing page
File: client/src/landing/components/WhatsAppWidgetSection.tsx
+55 11 5028-9356 has EXPIRED since 2026-03-26. Visitors clicking "Enviar mensagem de teste" hit a dead number.
Fix: update to active number or hide section.

### H-2 — PII not masked in secure-logger
File: api/_lib/secure-logger.js
phone, customer_phone, email, customer_email, customer_name NOT in SENSITIVE_KEYS.
Raw PII logged throughout reservations, waitlist, WhatsApp handlers.

### H-3 — Unauthenticated Stripe PaymentIntent creation
File: api/create-deposit-intent.js:14
Public endpoint. Caller supplies restaurant_id with no auth.
Anyone can create PaymentIntents against any restaurant. Require signed booking token.

### H-4 — ES locale: 125 missing translation keys
File: client/src/i18n/locales/es.json
Entire features in English for Spanish users: Events page (36 keys), Floor Plan (11 keys),
demo banner/voice UI, satisfaction survey (22 keys), settings strings.

### H-5 — ElevenLabs agents leak on demo cleanup
File: api/cron/cleanup-expired-demos.js
Cron deletes DB rows but never calls deleteAgent(demo.id).
Every expired demo leaves an orphaned paid ElevenLabs agent.
Fix: add deleteAgent call before deleting restaurant_config.

### H-6 — NoPlanPricing hardcoded in PT-BR
File: client/src/pages/SubscriptionManage.tsx:257-383
Entire upgrade pricing component hardcoded Portuguese. EN/ES users see PT during upgrade flow.

### H-7 — Design system: green colors outside WhatsApp mockup
Files: WhatsAppWidgetSection.tsx, VoiceWidgetSection.tsx, LandingPage.tsx
- text-[#0B7A2F] section label in WhatsApp section
- bg-whatsapp CTA button in VoiceWidgetSection (not a WhatsApp action)
- bg-whatsapp green dot on social proof badge
Green inside phone mockup (simulating WhatsApp) is fine. Outside = violation.

### H-8 — waha-webhook.js signature verification unconfirmed
File: api/waha-webhook.js
All other webhooks verified. WAHA not confirmed. Needs manual check.

---

## MEDIUM

### M-1 — Scroll hijack in InlineDemoSection
Scrolling over the inline demo on desktop navigates to /demo?preset=brazilian&embed=true.
Iframe capturing scroll events. Needs scroll passthrough fix.

### M-2 — InlineDemoSection missing ErrorBoundary on landing page
File: client/src/landing/pages/LandingPage.tsx:81-82
Primary desktop demo has no ErrorBoundary. Wrap with ErrorBoundary silent.

### M-3 — Old email address still in UI and emails
Files: SubscriptionManage.tsx:213,375, TermsOfService.tsx:168, PrivacyPolicy.tsx:34,126,145, api/_lib/email.js:158,196,234,493
seatable.ai.br@gmail.com still referenced. Should be hello@seatable.one.
Also email footers hardcoded English regardless of user language.

### M-4 — shadow-2xl on InlineDemoSection card
Design spec: zero shadows on cards. The inline demo container has shadow-2xl. Remove it.

### M-5 — scrape-restaurant.js has no auth
File: api/scrape-restaurant.js
External API call (Google Places) with no auth. Anyone uses Seatable's quota for free.

### M-6 — push-subscribe.js no ownership validation
File: api/push-subscribe.js
Anyone knowing a reservation_id UUID can register push subscription.
Add phone/email ownership check.

### M-7 — Cron dedup gap
Files: api/cron/send-campaigns.js, api/cron/manager-briefings.js
No cooldown check before LLM calls. Vercel retry = double execution.

### M-8 — report-usage.js accepts user JWT
File: api/report-usage.js:49
Any authenticated user can trigger Stripe metered billing reporting. Should be CRON_SECRET only.

### M-9 — 2.5s blank loading state on booking page
/book/cantina-bella-vista shows blank white + spinner. Add skeleton.

### M-10 — Stripe Checkout locale hardcoded English
File: api/create-checkout-session.js:126
locale: 'en' — Brazilian customers see Stripe in English. Change to 'pt-BR' or 'auto'.

### M-11 — timingSafeEqual throws on malformed sig
File: api/campaign-unsubscribe.js:28
Invalid hex sig -> wrong-length buffer -> timingSafeEqual throws -> 500.
Wrap in try-catch, return 400.

### M-12 — CRON_SECRET overloaded on elevenlabs-webhook
File: api/elevenlabs-webhook.js:85
CRON_SECRET shared with 20+ crons. Introduce dedicated ELEVENLABS_TOOL_SECRET.

---

## LOW

### L-1 — VoiceFilters search placeholder not i18n'd
File: client/src/components/voice/VoiceFilters.tsx:84
placeholder="Search voices..." hardcoded English.

### L-2 — ml_interventions not cleaned on demo delete
File: api/cron/cleanup-expired-demos.js:91
.eq('restaurant_id') on table with no restaurant_id column. Silently deletes 0 rows.

### L-3 — maxDuration 120s on 2 endpoints
manager-chat.js and twilio-voice-connect.js at 120s. Double cost vs 60s default.

### L-4 — LaunchChecklistModal does not auto-validate
3 manual checkboxes with no real validation of agent state, WhatsApp, plan.

### L-5 — event-public.js: no is_active filter
File: api/event-public.js:57
Exposes inactive events. Add .eq('is_active', true).

### L-6 — guest-feedback.js uses select('*')
File: api/guest-feedback.js:71
Tech debt TODO comment. Not a security risk but schema not finalized.

### L-7 — Live indicator dot convention inconsistency
Some live dots green #10b981, others rose/red. Pick one.

### L-8 — Rate limit falls back silently when Redis down
File: api/_lib/rate-limit.js:112
Per-instance fallback on Redis failure. Should log CRITICAL, not INFO.

### L-9 — "5 restaurantes ingressaram esta semana" is static
Hardcoded social proof counter. Make dynamic or remove.

---

## Verified Clean

- Auth middleware on all protected endpoints
- Multi-tenant restaurant_id scoping throughout
- Stripe/Meta/Twilio webhook signatures all verified
- All crons protected with CRON_SECRET
- CORS locked to seatable.one (no wildcard)
- No SQL injection vectors
- No raw console.log in API code
- PT-BR i18n fully in sync with EN
- Phase 12 substantively complete
- Phase 13 complete
- All PostHog analytics events wired
- ErrorBoundary on all routes in App.tsx
- Manager AI full-screen interface built

---

## Fix Checklist (priority order)

- [x] C-1: Rotate hardcoded ANON_KEY (reels-toolkit/scripts/record-scenes.js) — uses process.env.SUPABASE_ANON_KEY now. **ACTION NEEDED: rotate key in Supabase dashboard → Project Settings → API → Regenerate ANON key**
- [x] C-2: Remove fallback-secret (campaign-unsubscribe.js + automatedCampaignService.js) — throws if CRON_SECRET missing
- [x] H-1: Fix expired WhatsApp number on landing page — updated to 551150282009. **ACTION NEEDED: verify WhatsApp Business activated on new number**
- [x] H-2: Add phone/email to secure-logger SENSITIVE_KEYS — phone, customer_phone, email, customer_email, customer_name, name all masked
- [x] H-5: Delete ElevenLabs agent in cleanup-expired-demos.js — deleteAgent() called before restaurant_config deletion
- [x] H-6: NoPlanPricing fully i18n'd — NO_PLAN_TIERS moved inside component, all 8 hardcoded PT strings replaced with t(); 8 new keys added to EN/ES/PT-BR locales
- [x] H-7: Fix green design violations — section label, CTA button, pulse dot changed to burgundy in WhatsAppWidgetSection, VoiceWidgetSection, LandingPage; phone number text also updated to 5028-2009
- [ ] H-4: Backfill ES 125 missing translation keys (deferred — large scope)
- [x] M-1: Fix scroll hijack — click-to-interact overlay added; iframe has pointer-events:none until user clicks
- [x] M-2: InlineDemoSection wrapped in ErrorBoundary silent in LandingPage.tsx
- [x] M-3: Replace seatable.ai.br@gmail.com → hello@seatable.one (5 files, 11 occurrences)
- [x] M-4: Remove shadow-2xl from InlineDemoSection card
- [x] M-8: Restrict report-usage.js to CRON_SECRET only (admin JWT path removed)
- [x] M-10: Stripe Checkout locale: 'en' → 'auto' (browser-detected)
- [x] H-3: Add auth to create-deposit-intent.js — new api/booking-token.js (HMAC, 10-min window); BookingForm fetches token before PaymentIntent creation. **ACTION NEEDED: add BOOKING_HMAC_SECRET to Vercel env vars**
- [x] L-1: VoiceFilters search placeholder i18n'd — useTranslation + t('voice.searchVoices') with fallback
- [x] L-2: Fix ml_interventions cleanup — fetch reservation IDs first, delete .in('reservation_id', ids)
- [x] L-5: event-public.js now filters .eq('is_active', true) — inactive events no longer exposed
- [x] L-8: rate-limit.js Redis fallback minimum log level raised from info → warn
