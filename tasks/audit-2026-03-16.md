# Seatable Platform Audit — 2026-03-16

## Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 3 |
| HIGH | 8 |
| MEDIUM | 14 |
| LOW | 10 |
| TOTAL | 35 |

7 agent runs: Backend API (x2), Security (x2), Data/Cron (x2), Frontend E2E (x2), UI/UX.
All tests pass: 1,434 backend + 481 frontend. Build clean. 0 npm vulnerabilities.

---

## CRITICAL (3)

### C-01: Onboarding deletes ALL restaurants' tables (cross-tenant destruction)
`api/onboarding/complete.js:254-257` — `.delete().neq('id', '00000000-...')` with **no restaurant_id filter**. Uses supabaseAdmin (bypasses RLS).

### C-02: Pricing API has zero restaurant_id scoping
`api/pricing.js` — All operations (rules CRUD, events, analytics, stats) query globally. Any authenticated user can read/modify/delete any restaurant's pricing data.

### C-03: ml-performance.js leaks cross-tenant ML data
`api/ml-performance.js` — receives restaurant_id in query but never passes it to 5 query functions. All data returned is global.

---

## HIGH (8)

### H-01: ml-outcomes.js cross-tenant reservation access
`api/ml-outcomes.js:119-123` — Fetches reservation by UUID without restaurant_id filter.

### H-02: batch-predict.js cross-tenant customer history
`api/batch-predict.js:42-56` — Queries customer_history by phone/email without restaurant_id.

### H-03: Demo cleanup doesn't cascade to dependent tables
`api/cron/cleanup-expired-demos.js` — Only deletes reservations, tables, restaurant_config. Leaves orphaned rows in 9+ tables.

### H-04: Error messages leak internal details
`api/cron/check-late-reservations.js:66` (details: error.message), `cleanup-expired-demos.js:156`, `analytics.js:38,63`.

### H-05: send-campaigns/send-feedback lack CRON_SECRET null check
If CRON_SECRET undefined, `Bearer undefined` passes auth.

### H-06: Live demo page not mobile responsive
`/live-demo` two-column layout doesn't stack on mobile (375px). Dashboard extends beyond viewport, requires horizontal scroll.

### H-07: Oversized files (7 files >800 lines)
twilio-whatsapp-webhook.js (1700), customerDNA.js (1002), db-tables.js (986), db/tables.js (904), DashboardWalkthroughSection.tsx (974), tool-handlers.js (838), phone-integration-simple.js (835).

### H-08: Login page two-column layout doesn't collapse on mobile
Desktop split layout (branding + form) doesn't stack vertically at 375px.

---

## MEDIUM (14)

### M-01: JWT_SECRET silently falls back to SUPABASE_JWT_SECRET
### M-02: CORS sends Access-Control-Allow-Credentials for untrusted origins
### M-03: manager-chat.js missing CORS headers / OPTIONS handling
### M-04: demo-chat.js prompt injection via unsanitized context object
### M-05: Inconsistent 401 error response formats (4 shapes)
### M-06: In-memory rate limit fallback useless on Vercel serverless
### M-07: check-late-reservations timezone-fragile time comparison
### M-08: Duplicate DB abstraction (db-tables.js legacy vs db/tables.js)
### M-09: proactive-comms storeOpportunities is a no-op (results lost)
### M-10: pre-reservation-upsell wrong logCronRun signature (3 args vs 2)
### M-11: Fragile auth pattern in import-history.js and manager-documents.js
### M-12: Schedule overlap on Mondays at 3 AM UTC (cleanup + refresh)
### M-13: Mixed EN/PT content — "Without AI" section, WhatsApp mockup, booking cancellation text
### M-14: Static date "March 15" hardcoded in live demo dashboard mock

---

## LOW (10)

### L-01: check-late-reservations overwrites reservation notes unconditionally
### L-02: Waitlist ID uses predictable Date.now()
### L-03: customer-reservation.js uses setWebhookCors instead of setInternalCors
### L-04: WhatsApp webhook logs full message body (PII in text)
### L-05: Twilio SMS viewer auto-refreshes with PII
### L-06: Health endpoint latency ~700ms
### L-07: Dead code: security.js references non-existent security-headers module
### L-08: report-usage.js uses x-vercel-cron header as alt auth
### L-09: 404 page not localized (always English)
### L-10: Phone placeholder shows Spanish format (+34) in English mode

---

## FALSE POSITIVES (from Playwright agents)

- "Stale build assets / blank page" — Vercel CDN cold start during Playwright test. Production loads in 0.12s.
- "SPA routing instability / redirect chaos" — Playwright rapid-fire navigation artifact. Routes correctly defined in App.tsx, no auth guards on public routes.
- "Demo preset links redirect to login" — DemoDashboard has no auth check. Cascade from asset loading failure.
- "/privacy and /terms pages missing" — Both exist at App.tsx:149-150, render PrivacyPolicy/TermsOfService components.
- "Booking page no time slots" — Config issue for test restaurant (no business hours), not a code bug.
- "/api/pricing returns 401" — This is the dynamic pricing admin API, not a public pricing page. Auth is correct.
- "push-subscribe accepts unauthenticated requests" — Intentionally public (customer push subscriptions after booking). Validates restaurant_id exists, rate limited.

---

## RESOLUTION STATUS

| Status | Count |
|--------|-------|
| FIXED | 25 |
| FALSE POSITIVE | 9 |
| DEFERRED | 1 |

### Fixed Items — Security (8)
- **C-01**: onboarding/complete.js — table delete scoped to restaurant_id
- **C-02**: pricing.js — all operations scoped with restaurant_id
- **C-03**: ml-performance.js — filtered through reservation IDs
- **H-01**: ml-outcomes.js — all 3 handlers scoped to req.user.restaurant_id
- **H-02**: batch-predict.js — customer_history queries scoped by restaurant_id
- **H-03**: cleanup-expired-demos.js — cascade deletes 10+ dependent tables before restaurant_config
- **H-04**: check-late-reservations.js + cleanup-expired-demos.js — error.message removed from responses
- **H-05**: send-campaigns.js + send-feedback.js — CRON_SECRET null check added

### Fixed Items — Dashboard Deep Audit (17)
- **K-1**: VoiceSettingsPage — removed non-functional VoiceExperimentPanel
- **K-2**: CallTrackingDashboard — wired export button with CSV download
- **K-3**: LanguageSettings — removed dead onLanguageChange callback
- **DB-H-1**: predictive-analytics.js — replaced hardcoded €45/cover with actual service_records avg
- **DB-H-2**: ltv.js — replaced hardcoded €45/cover with actual service_records avg
- **DB-H-3**: StrategyMetricsWidget — replaced hardcoded R$ format with formatCurrency()
- **DB-H-4**: AIStrategyPanel — removed hardcoded R$90 from placeholder
- **DB-H-5**: AIStrategyPanel — removed hardcoded R$ target reference
- **DB-S-1**: ActivityFeedWidget — replaced return null with empty state UI
- **DB-S-3**: RevenueStatsWidget — replaced return null with empty state UI
- **DB-S-4**: FeedbackWidget — added loading skeleton + empty state UI
- **DB-i-1**: FeedbackWidget — replaced hardcoded English with t() i18n calls
- **DB-i-2**: ManagerChatPanel — replaced hardcoded "Failed to send" with t()
- **DB-i-5**: ManagerNotesPanel — removed dead language prop, uses i18n internally
- **DB-U-1**: Dashboard Export button — changed to "Reports" with correct icon
- **DB-U-2**: WhatsAppSettingsPage — fixed hover:bg-spotify to hover:bg-whatsapp/80
- **DB-U-3**: ActivePartiesPanel — timer interval 60s → 15s for responsive updates
- **DB-U-4**: TableLayoutPanel — view mode persisted to localStorage
- **DB-U-5**: RestaurantSettingsPage — timezone raw text input → dropdown select
- **DB-U-7**: PhoneIntegrationPanel — replaced BR-specific placeholder with generic
- **DB-U-8**: StatsBar — fixed "AI Calls" mislabel → "Active Parties"

### Verified Non-Issues
- **H-06**: Live demo page already responsive (flex-col lg:flex-row)
- **H-08**: Login page already responsive (brand panel hidden lg:flex)
- **DB-S-5**: ReferralWidget returns null with no referral code (correct behavior)
- **DB-U-6**: confirm() for note deletion — functional, modal would be over-engineering

### Deferred
- **H-07**: Oversized files (7 files >800 lines) — cosmetic refactoring, separate PR
